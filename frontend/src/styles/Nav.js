import styled from "styled-components";

const Nav = styled.nav`
  ul {
    list-style: none;
    display: flex;
    gap: 2px;

    li {
      button {
        cursor: pointer;
      }
    }
  }
`

export default Nav